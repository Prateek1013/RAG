spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8080
          predicates:
            - Path=/api/auth/**
server:
  port: 8081

security:
  jwt:
    secret: "${JWT_SECRET_KEY}"